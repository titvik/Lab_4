@{
    ViewData["Title"] = "Check Box";
}

<h1>Check Box</h1>


<form method="POST">
    <div class="form-group w-25">
        <div class="checkbox">
            <label><input type="checkbox" name="CheckBoxState" value="true" checked> IsSelected</label>
        </div>
        <button type="submit" class="btn btn-light" >Submit</button>
  </div>
</form>